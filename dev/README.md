# Dev fixtures

This directory is for local `-dev` mode only.

Keys and configs here are **generated locally** on first run and are **not committed to git**.

Your production keys live on the server in:
- `/etc/wireguard/params`
- `/etc/wireguard/wg0.conf`
- `/root/wg0-client-*.conf`

Those files are never touched by git.
